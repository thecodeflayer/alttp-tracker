import {IStaticMapShopsDW} from '@/interfaces/IStaticMapShopsDW';

export class RetroStaticMapShopsDW implements IStaticMapShopsDW{
  dwDeathMtnShop = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwDeathMtnFairy = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwDesertFairy = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwDesertHint = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwPotionShop = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwLakeHyliaFairy = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwEastHint = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  podHint = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwForestShop = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwLumberjackHutShop = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwOutcastShop = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwSanctuaryHint = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwFortuneTeller = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwLakeHyliaShop = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  archeryGame = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwBonkFairy = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwLakeLedgeFairy = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwLakeLedgeHint = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
  dwLakeLedgeSpikeCave = {x:0, y:0, title:'',
    req:[], valid(items, dungeons): boolean{
      return true;
    }};
}
